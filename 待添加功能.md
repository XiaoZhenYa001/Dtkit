# 添加一个很特别的功能(尝试一下,做好万全准备后再添加)
设置界面的`**桌面整理**功能,当选中时,就可以按预设的规则或自定义的规则将桌的工具全部收纳到右上角一个隐形的控件下,点击/鼠标移动到指定位置就展开.(按类排好了)
额外的 可以搜索收纳的所有进来的文件或程序.
- [ ] 文件并入管理"界面"时(一个设置:并入动画),这时界面的图标就像流星一样入界里
- [ ] 快捷键打开
- [ ] 可以将指定文件夹的直接子内容展示在桌面上，相当于一层深度的展开，而完全归扫描 。"鼠标移动到该文件夹时展开对应的一级"文件""
- [x] 实现"状态栏图标",类似于右键该应用的小图标,调控该设置的各项小设置
- [x] 搜索框支持命令eg: "/p 1.txt"  是指在该文件类型下搜索
- [ ] 软件图标显示
- [ ] 二级菜单展开优化


### 收藏页面
- [x] 收藏页面的工具顺序可拖动排序
- [ ]  收藏页面拖动工具直窗体外时(并松开),弹出对话框"是否剥离功能",确定后就生成一个html文件(包含实现代码与css效果)

### 工具
- [x] 二维码工具
- [x] 实现下载(二维码)功能
- [ ] 二维码批量生成功能,通过导入csv等文件一键生成并保存到对应的文件夹中

### 定时操作
- [ ] **不同的时间有不同的风格(例如:立春了,就限定春日定时器)**
- [ ] 实现右侧拖拽不同任务以切换顺序
- [x] 实现任务不同状态不同风格css效果
- [x] 更改整个css效果
- [x] 响铃提示声音的优化
- [x] 可添加定时执行文件(开机执行某个文件等等)
- [ ] 扩展:自主分析软件安装目录的/Kit/clock文件夹的文件.这些文件在"选择提示音"的位置显示出来.然后可以选择.(例如每个重要的工具各自一个文件夹,相关的内容/文件等用户自定义)
- [ ] 有一个按钮,点击时弹出该工具的注意信息(首次进入时也弹出)(建议一个新的文件,以便好操作样式与内容.原本的那个文件太大了)
- [ ] 有个大问题:闹钟响零不是这样的......怎么变为"打开"文件了....


### 设置界面
#### 是独立的,它的展示跳转到该设置界面(目的防止**单个文件太大**)
- [x] 添加保存地址设置(将下载到选择的文件夹内)
#### 定时操作每天几点"清理垃圾"(不一定是这个),定时执行某文件等

### 下载界面
- [x] 下载普通文件
- [ ] 下载特殊文件
- [ ] 存在有时在下载出现提示无法下载成功(但是重试后可以下载成功)
- [ ] 下载一些国内网络不可以直接下载的东西(或者说很慢的东西)(通过该页面下载)(配置镜像源实现下载docker等不可直接下载的东西)
- [ ] 下载完成后会做文件完整性校验（比如检查大小、哈希或官方提供的校验信息），避免“下完了却坏了”的情况


##### 可以弄一些推荐应用(或者不弄.就干净的界面)

## 添加各种各样的小彩蛋,例如一个想不到的位置点击弹出聊天群

## 尝试实现一个不同的多用户交流的功能,不是类似于现在的(还不如没有)

## 设置界面有一个截图,快捷键,可以实现截图
## 优化

## 颜色提取器工具(可以绑定快捷键,这个快捷键一旦点击,此时鼠标在一个地方进行点击就记录下此处的颜色(假想))


### 是不是可以尝试添加密码管理的界面,然后把我的PassCard导入进来

### 本地脚本管理(可以管理自己电脑上的脚本(自启动什么的))

## 自定义右键

益智小游戏之类的