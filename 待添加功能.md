# 添加一个很特别的功能(尝试一下,做好万全准备后再添加)
    `设置界面的`**桌面整理**功能,当选中时,就可以按预设的规则或自定义的规则将桌面的工具全部收纳到右上角一个隐形的控件下,点击/鼠标移动到指定位置就展开.(按归类排好了)
        额外的 可以搜索收纳的所有进来的文件或程序.

### 收藏页面
- []收藏页面的工具顺序可拖动排序
- [] 收藏页面拖动工具直窗体外时(并松开),弹出对话框"是否剥离功能",确定后就生成一个html文件(包含实现代码与css效果)

### 工具
- [x] 二维码工具
- [x] 实现下载(二维码)功能
- [] 二维码批量生成功能,通过导入csv等文件一键生成并保存到对应的文件夹中

### 定时操作
- [] **不同的时间有不同的风格(例如:立春了,就限定春日定时器)**
- [] 实现右侧拖拽不同任务以切换顺序
- [] 实现任务不同状态不同风格css效果
- [] 更改整个css效果
- [] 响铃提示声音的优化
- [x] 可添加定时执行文件(开机执行某个文件等等)



### 设置界面
#### 是独立的,它的展示跳转到该设置界面(目的防止**单个文件太大**)
#### 添加保存地址设置(将下载到选择的文件夹内)
#### 定时操作每天几点"清理垃圾"(不一定是这个),定时执行某文件等

### 下载界面
#### 下载一些国内网络不可以直接下载的东西(或者说很慢的东西)(通过该页面下载)(配置镜像源实现下载docker等不可直接下载的东西)

## 添加各种各样的小彩蛋,例如一个想不到的位置点击弹出聊天群

## 尝试实现一个不同的多用户交流的功能,不是类似于现在的(还不如没有)

## 设置界面有一个截图,快捷键,可以实现截图
## 优化


### 是不是可以尝试添加密码管理的界面,然后把我的PassCard导入进来

### 本地脚本管理(可以管理自己电脑上的脚本(自启动什么的))

益智小游戏之类的