# 桌面整理功能 - 设计文档

> 创建日期：2025年12月31日  
> 状态：设计阶段

---

## 一、功能概述

桌面整理是 DtKit 的主推功能，作为独立窗口运行。用户开启后，鼠标移动到屏幕右上角热区可触发侧边栏，提供桌面文件的分类浏览和全局搜索功能。

### 核心特点
- 独立窗口，主窗口关闭后仍可运行
- 热区触发，不干扰正常桌面使用
- 分类管理，快速定位文件
- 全局搜索，支持命令模式
- 整理建议，用户确认后执行

---

## 二、交互设计

### 2.1 触发方式

| 项目 | 规格 |
|------|------|
| 热区位置 | 屏幕右上角 |
| 热区距右 | 0px（紧贴右边缘） |
| 热区距顶 | 0px |
| 热区大小 | 240px × 5px |
| 触发延迟 | 300ms（鼠标停留） |
| 收起延迟 | 200ms（鼠标离开后） |
| 收起条件 | 鼠标离开侧边栏区域 |
| 展开时热区 | 不响应（避免重复触发） |

### 2.2 侧边栏规格

| 项目 | 规格 |
|------|------|
| 位置 | 屏幕左侧，距左边缘 20px |
| 默认尺寸 | 550px × 450px |
| 最大宽度 | 屏幕宽度的 45% |
| 最大高度 | 屏幕高度的 60% |
| 可调整 | 用户可拖拽左下角或右下角改变大小 |
| 拖拽方向 | 向左拖=向左扩展，向右拖=向右扩展 |
| 展开动画 | 从顶部向下滑出（约 200ms） |
| 收起动画 | 向上滑入（约 150ms） |

### 2.3 样式

```css
/* 暗色毛玻璃 */
--panel-bg: rgba(30, 30, 35, 0.85);
--panel-blur: blur(20px);
--category-bg: rgba(255, 255, 255, 0.05);
--category-hover: rgba(255, 255, 255, 0.1);
--text-primary: #ffffff;
--text-secondary: rgba(255, 255, 255, 0.6);
--accent: #4a9eff;
--border-radius: 12px;
```

---

## 三、功能模块

### 3.1 搜索框（顶部）

- 位置：侧边栏顶部
- 占位符：`搜索文件或输入命令...`
- 支持命令模式

#### 命令格式

| 命令 | 含义 | 示例 |
|------|------|------|
| `/d` | 文档类 | `/d 报告` |
| `/p` | 图片类 | `/p wallpaper` |
| `/v` | 视频类 | `/v 教程` |
| `/f` | 文件夹 | `/f 项目` |
| `/a` | 应用程序 | `/a chrome` |
| `/z` | 压缩包 | `/z backup` |
| 无前缀 | 全局搜索 | `readme` |

### 3.2 分类列表

| 分类 | 图标 | 包含类型 |
|------|------|----------|
| 最近使用 | ⏱️ | 按访问时间排序，最多显示 7 个 |
| 文档 | 📄 | .doc, .docx, .pdf, .txt, .md, .xls, .xlsx, .ppt 等 |
| 图片 | 🖼️ | .jpg, .jpeg, .png, .gif, .bmp, .svg, .webp 等 |
| 视频 | 🎬 | .mp4, .avi, .mkv, .mov, .wmv 等 |
| 音频 | 🎵 | .mp3, .wav, .flac, .aac 等 |
| 压缩包 | 📦 | .zip, .rar, .7z, .tar, .gz 等 |
| 程序 | 💻 | .exe, .msi, .lnk（快捷方式）等 |
| 文件夹 | 📂 | 目录 |
| 其他 | 📎 | 未分类文件 |

#### 交互
- 点击分类：展开/折叠文件列表
- 悬停文件夹：显示一级子内容预览
- 双击文件：打开文件
- 右键文件：显示上下文菜单

### 3.3 右键菜单

| 选项 | 图标 | 功能 |
|------|------|------|
| 打开 | 📂 | 使用默认程序打开 |
| 定位到文件 | 📍 | 在资源管理器中显示 |
| 复制 | 📋 | 复制文件 |
| 剪切 | ✂️ | 剪切文件 |
| 重命名 | ✏️ | 重命名文件 |

> 注：暂不包括删除功能

### 3.4 整理建议（可选）

- 需在设置中开启"自动分析"
- 分析完成后显示建议数量徽章
- 点击展开建议列表
- 用户勾选后点击"执行选中项"

---

## 四、设置项

### 4.1 主开关

位置：设置界面

```
┌─────────────────────────────────────┐
│ 桌面整理                    [开关]  │
│   └─ ☐ 自动分析                     │
└─────────────────────────────────────┘
```

### 4.2 开启流程

1. 用户点击开关
2. 弹出确认弹窗：
   - 若勾选"自动分析"：提示将自动分析桌面文件
   - 若未勾选：提示将开启桌面整理功能
3. 用户确认后：
   - 启动后台热区监听
   - **需要重启应用生效**

### 4.3 关闭流程

1. 用户关闭开关
2. 弹出确认弹窗
3. 确认后停止热区监听

### 4.4 用户偏好（本地存储）

```json
{
  "desktopOrganizer": {
    "enabled": true,
    "autoAnalyze": false,
    "panelWidth": 550,
    "panelHeight": 450,
    "expandedCategories": ["documents", "images"],
    "hotZoneDelay": 300
  }
}
```

---

## 五、技术架构

### 5.1 文件结构

```
src/
├── desktop-organizer/              ← 前端独立模块
│   ├── index.html                  ← 独立窗口入口
│   ├── main.js                     ← 主逻辑
│   ├── styles.css                  ← 样式
│   ├── components/
│   │   ├── SearchBar.js            ← 搜索框
│   │   ├── CategoryList.js         ← 分类列表
│   │   ├── FileItem.js             ← 文件项
│   │   ├── ContextMenu.js          ← 右键菜单
│   │   └── SuggestionPanel.js      ← 整理建议
│   └── utils/
│       ├── fileClassifier.js       ← 文件分类逻辑
│       └── searchEngine.js         ← 搜索引擎

src-tauri/
├── src/
│   ├── desktop/                    ← Rust 后端模块
│   │   ├── mod.rs                  ← 模块入口
│   │   ├── scanner.rs              ← 桌面扫描
│   │   ├── hotzone.rs              ← 热区监听
│   │   ├── watcher.rs              ← 文件变化监听
│   │   └── commands.rs             ← Tauri 命令
│   ├── lib.rs                      ← 引入模块
│   └── main.rs
```

### 5.2 Tauri 窗口配置

```json
{
  "label": "desktop-organizer",
  "title": "桌面整理",
  "width": 550,
  "height": 450,
  "resizable": true,
  "transparent": true,
  "decorations": false,
  "alwaysOnTop": true,
  "visible": false,
  "skipTaskbar": true
}
```

### 5.3 核心功能

#### 热区监听（Rust）
- 使用 `windows` crate 监听全局鼠标位置
- 检测鼠标进入热区并停留 300ms
- 触发窗口显示/隐藏

#### 桌面扫描（Rust）
- 获取桌面路径：`dirs::desktop_dir()`
- 扫描范围：仅桌面一级目录
- 返回文件列表及元数据

#### 文件变化监听（Rust）
- 使用 `notify` crate 监听桌面目录
- 文件增删改时自动刷新列表

#### 文件操作（Rust）
- 打开文件：`opener::open()`
- 定位文件：`Command::new("explorer") /select`
- 复制/剪切：写入剪贴板
- 重命名：`std::fs::rename()`

---

## 六、实现步骤

### 阶段一：基础框架
- [x] 创建独立窗口配置
- [x] 实现基础 UI（搜索框 + 分类列表）
- [x] 桌面文件扫描
- [x] 文件分类逻辑

### 阶段二：热区触发
- [x] 实现全局鼠标位置监听
- [x] 热区检测与延迟触发
- [x] 窗口显示/隐藏动画

### 阶段三：交互完善
- [x] 分类展开/折叠
- [x] 文件双击打开
- [x] 右键菜单
- [x] 文件夹一级预览

### 阶段四：搜索功能
- [x] 全局搜索
- [x] 命令模式解析
- [x] 搜索结果高亮

### 阶段五：设置集成
- [x] 设置界面开关
- [x] 用户偏好存储
- [x] 确认弹窗

### 阶段六：高级功能
- [ ] 文件变化自动监听
- [ ] 整理建议分析
- [x] 拖拽调整窗口大小

### 阶段七：系统托盘（后续）
- [ ] 托盘图标
- [ ] 右键菜单快捷控制

---

## 七、注意事项

1. **独立运行**：主窗口关闭后，桌面整理功能仍需运行
2. **性能考虑**：热区监听需低资源占用
3. **用户体验**：动画流畅，响应及时
4. **配置持久化**：用户偏好需保存到本地
5. **重启生效**：开启/关闭功能需重启应用

---

## 八、参考

- 原型图：`desktop-organizer-prototype.html`
- 待添加功能：`待添加功能.md`
