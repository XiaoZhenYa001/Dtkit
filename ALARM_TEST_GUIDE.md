# 定时闹钟优化功能测试指南

## 新增功能概览

### 1. 全类型任务暂停/恢复功能
- **倒计时任务**：暂停时停止倒计时，恢复后从暂停点继续
- **固定时间任务**：暂停时到点不提醒，恢复后正常检测
- **整点报时任务**：暂停时到点不提醒，整点过后显示"整点已过"
- **间隔提醒任务**：暂停时停止定时器，恢复后重新计时

### 2. 音频文件播放系统
- 支持真实音频文件播放（.wav, .mp3, .ogg, .m4a）
- 音频循环播放7分钟
- 队列机制：同时只播放一个音频
- 带"停止闹钟"按钮的精美Toast通知

### 3. 视觉状态优化
- **运行中**：蓝/紫/青色渐变，呼吸灯效果
- **已暂停**：黄色渐变，半透明暂停图标水印
- **已停止**：灰色，80%灰度滤镜
- **已完成**：显示"重新开始"按钮

## 测试步骤

### 测试1：音频文件加载
1. 将音频文件放入应用数据目录：
   - Windows: `C:\Users\<用户名>\AppData\Roaming\com.administrator.dtkit\Kit\clock\`
   - 或通过界面"打开文件夹"按钮快速访问
2. 打开定时闹钟工具
3. 选择"播放提示音"动作
4. 检查是否显示音频列表
5. 点击预览按钮测试播放

### 测试2：倒计时暂停功能
1. 创建一个5分钟的倒计时任务
2. 等待1分钟后点击"暂停"按钮
3. 观察：
   - 卡片变为黄色
   - 倒计时停止
   - 显示"已暂停"徽章
   - 出现半透明暂停图标水印
4. 等待30秒后点击"恢复"按钮
5. 观察倒计时从暂停点继续

### 测试3：固定时间暂停功能
1. 创建一个1分钟后的固定时间任务
2. 立即点击"暂停"按钮
3. 等待时间到达
4. 确认没有触发提醒
5. 点击"恢复"按钮
6. 确认下一次定时正常触发

### 测试4：音频播放与队列
1. 创建3个10秒倒计时任务，都设置为"播放提示音"
2. 等待所有任务触发
3. 观察：
   - 只有一个音频在播放
   - 显示精美的Toast通知
   - Toast中有"停止闹钟"按钮
4. 点击"停止闹钟"按钮
5. 确认音频停止，队列中下一个音频开始播放

### 测试5：整点报时时间已过
1. 创建整点报时任务（如果当前是10:30，设置为每小时报时）
2. 等待到11:05（整点已过）
3. 观察任务卡片显示"整点已过"
4. 确认无法暂停/恢复（按钮被禁用）

### 测试6：重新开始已完成任务
1. 创建30秒倒计时任务
2. 等待任务完成
3. 观察：
   - 卡片变灰
   - 按钮变为"重新开始"图标（刷新图标）
4. 点击"重新开始"
5. 确认倒计时重置并重新开始

### 测试7：内存泄漏检查
1. 创建多个任务并反复暂停/恢复
2. 播放音频并多次停止
3. 切换到其他工具标签页再回来
4. 使用浏览器开发工具检查内存占用
5. 确认没有明显的内存泄漏

## 预期结果

✅ 所有任务类型支持暂停/恢复  
✅ 音频正确循环播放7分钟  
✅ 同时只有一个音频播放  
✅ Toast通知可以停止音频  
✅ 暂停状态视觉清晰可辨  
✅ 整点已过正确显示  
✅ 重新开始功能正常  
✅ 无内存泄漏

## 已知限制

1. 音频文件需手动复制到应用数据目录（首次使用）
2. 新增音频后需重新打开工具才能加载
3. 音频播放时长固定为7分钟
4. 音频队列按触发顺序依次播放，无法调整优先级

## 故障排除

### 问题：看不到音频文件
**解决方案**：
1. 检查音频文件是否在正确的目录
2. 确认文件格式是否支持（.wav, .mp3, .ogg, .m4a）
3. 重新打开定时闹钟工具

### 问题：音频无法播放
**解决方案**：
1. 检查文件是否损坏
2. 尝试预览播放功能
3. 查看浏览器控制台错误信息

### 问题：暂停后无法恢复
**解决方案**：
1. 检查任务是否仍处于启用状态
2. 尝试删除任务重新创建
3. 刷新页面

## 开发说明

### 关键代码位置
- **Tauri命令**：`src-tauri/src/lib.rs` (scan_audio_files, open_audio_folder)
- **音频播放**：`src/tools/alarm-clock/index.js` (playAudioLoop函数)
- **暂停逻辑**：`src/tools/alarm-clock/index.js` (pauseTask, resumeTask函数)
- **UI渲染**：`src/tools/alarm-clock/index.js` (createTaskCard函数)
- **CSS样式**：`src/tools/alarm-clock/index.js` (getStyles函数中的.paused类)

### 内存管理
- `activeLoopControllers: Map` - 存储音频控制器
- `currentPlayingAudio` - 当前播放的音频引用
- `audioQueue: []` - 待播放音频队列
- `AbortController` - 管理事件监听器生命周期

### 清理机制
- `stopTask()` - 清理任务定时器和音频控制器
- `destroy()` - 清理UI事件监听器和预览音频
- `stopAllAudio()` - 停止所有音频播放
