# 闹钟音频文件设置说明

## 音频文件位置

定时闹钟工具的音频文件存储在**应用数据目录**下的 `Kit/clock/` 文件夹中。

### Windows系统路径
```
C:\Users\<用户名>\AppData\Roaming\com.administrator.dtkit\Kit\clock\
```

## 初始音频文件

项目包含以下默认音频文件（位于项目根目录的 `Kit/clock/` 中）：
- 公鸡鸣叫.wav
- 噔噔噔.WAV

## 首次运行

应用首次运行时，Tauri会自动：
1. 创建应用数据目录下的 `Kit/clock/` 文件夹
2. 用户可以手动将项目根目录的音频文件复制到应用数据目录

## 添加自定义音频

### 方法1：通过应用界面
1. 打开"定时闹钟"工具
2. 选择"播放提示音"动作
3. 点击"打开文件夹"按钮
4. 将音频文件复制到打开的文件夹中
5. 重新打开工具即可看到新音频

### 方法2：手动复制
1. 找到应用数据目录（见上方路径）
2. 将音频文件复制到 `Kit/clock/` 文件夹
3. 重新打开工具

## 支持的音频格式
- `.wav` - WAV音频（推荐）
- `.mp3` - MP3音频
- `.ogg` - OGG音频
- `.m4a` - M4A音频

## 注意事项
- 音频文件会循环播放7分钟
- 建议使用时长在5秒以内的音频文件
- 文件名将直接显示在选择界面，建议使用有意义的名称
